# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## プラットフォーム

- **対象**: iOS (iOS 16+)
- **フレームワーク**: Expo (React Native) ※iOSアプリ本体
- **Apple Watch**: Swift (SwiftUI + WatchKit) ※watchOSはネイティブ実装が必須
- **配信**: App Store（審査通過が必須）

## 言語・ツールチェーン

- **言語**: TypeScript (strict: true)
- **パッケージマネージャー**: npm
- **リンター / フォーマッター**: ESLint + Prettier
- **テスト**: Jest

## コメント・ドキュメント

- コード中のコメントは**日本語**で書く
- 変数名・関数名・型名などの識別子は**英語**
- コミットメッセージは**日本語で自由記述**

## 命名規則

- 変数・関数: `camelCase`
- 型・クラス・インターフェース: `PascalCase`
- 定数(enum的な値): `UPPER_SNAKE_CASE`

## 開発手法: TDD（テスト駆動開発）

ビジネスロジック・ユーティリティ関数には必ずTDDを適用する。UIコンポーネントは対象外（後からテスト追加でもよい）。

### サイクル
1. **仕様の対話**: ユーザーと対話しながらテストケースを設計する
2. **Red**: 合意したテストケースを先に書く（この時点で失敗する）
3. **Green**: テストを通す最小限の実装を書く
4. **Refactor**: テストが通る状態を維持しつつリファクタリング

### テスト作成の進め方（対話形式）
新しい機能を追加する際、以下の流れでテストを作成する：
1. ユーザーに機能の目的・期待する振る舞いをヒアリングする
2. 想定されるテストケース（正常系・異常系・境界値）を提案する
3. ユーザーの確認・修正を受けてテストコードを確定する
4. 確定したテストを書いてから、そのテストを通す実装に進む

**テストが仕様書の役割を果たす** — 実装はテストで定義された振る舞いに従う。

### ルール
- 新しいロジックを実装する前に、必ず対応するテストファイルを先に作成する
- テストファイルは対象ファイルと同階層に `*.test.ts` として配置する
- 1つのテストが通るまで次の機能に進まない
- リファクタリング時は既存テストがすべてグリーンであることを確認してから行う

## タスク管理（トークン節約）

プランが承認された作業では、**規模の大小に関わらず必ず** `tasks.md` を作成する。

- 作業開始時に `tasks.md` があれば読んで続きから再開する
- 区切りのよいところで状態を `tasks.md` に書き、ユーザーに `/clear` を促す
- `tasks.md` には「次に何をすべきか」と「必要なコンテキスト（ファイルパス等）」を書く
- 全完了時は `tasks.md` を削除する

### プランモードの運用（必須・厳守）

新しい機能追加や複数ステップの作業を開始する際は、**必ずプランモード（EnterPlanMode）に入る**こと。
**コードの探索・調査だけでは実装に入ってはならない。プランファイルを書き、ユーザー承認を得るまで一切の実装禁止。**

#### 手順（この順序を必ず守ること）

1. **EnterPlanMode**: 作業開始時にプランモードに入る
2. **調査**: コードベースを探索し、必要な情報を集める
3. **プランファイル作成（必須）**: 調査結果をもとに、以下の構成でプランファイルを書く
   - **背景・目的**: なぜこの作業が必要か
   - **Phase一覧**: 作業をPhaseに分割し、各Phaseにサブタスクを `- [ ]` 形式で列挙する
   - **検証方法**: 完了をどう確認するか（テスト実行、ビルド確認など）
4. **ExitPlanMode**: プランを提示し、ユーザーの承認を待つ
5. **承認後・最初の操作**: プランファイルの内容を **`Write` ツールで `tasks.md` に書き出す**（規模に関わらず必須。これが最初のDiscord通知トリガーになる）
6. **実装開始**: tasks.md を書いた後に初めてコード編集に入る
7. **タスク単位で進捗記録**: サブタスク完了ごとに `tasks.md` のチェックを `[x]` に更新する（→ Discord通知が自動送信される）

> **注意**: プランモード中（EnterPlanMode〜ExitPlanMode）は Edit/Write が無効なため、tasks.md は更新できない。
> Discord通知はプランモード終了後、tasks.md を Write した時点から始まる。

#### 禁止事項
- プランファイルを書かずに `ExitPlanMode` を呼ぶこと
- ユーザー承認前にコードを編集・作成すること
- 調査だけして「問題なさそう」と判断し、プランを省略すること
- 承認後に tasks.md を書く前にコード編集を始めること（通知が飛ばなくなる）

### Discord通知（フック設定済み・厳守）

以下のタイミングで自動的にDiscordへ通知が送られる。ユーザーは離席中でも進捗を把握できる。

- **tasks.md 作成時**: 承認後に `Write` で tasks.md を作成した時点で初回通知が飛ぶ
- **タスク完了時**: `tasks.md` のチェックを更新するたびに進捗通知が飛ぶ（例: 「タスク完了 ✅ 3/10」）。**そのため、サブタスク完了時は必ず `tasks.md` を更新すること**
- **質問時**: `AskUserQuestion` を使うとDiscordに質問内容が通知される。**ユーザーへの確認・判断が必要な場合は、テキストで聞くのではなく必ず `AskUserQuestion` ツールを使うこと**（テキストだけの質問はDiscord通知が飛ばないため、ユーザーが気づけない）
- **セッション終了時**: 中断/完了の状態がDiscordに通知される

## コーディング原則

### 不変性（Immutability）の徹底
- stateやpropsを直接書き換えない
- 更新は必ず新しいオブジェクトを作成して行う（スプレッド構文、`Object.assign`等）

### 副作用の分離
- 通信やログ出力などの副作用は`useEffect`やカスタムフックに閉じ込める
- 計算ロジックは純粋関数として分離する

### 宣言的なリスト操作
- `for`ループの代わりに`map`, `filter`, `reduce`を使う
- 命令的なループは原則使用しない

## App Store審査対応ルール

### プライバシー (Apple Review Guideline 5.1)
- ユーザーデータはローカル保存のみ。外部サーバーへの送信を行わない
- トラッキングSDKは使用しない（ATT対応不要）
- `NSPrivacyAccessedAPITypes`をInfo.plistに正しく宣言する（Required Reason API対応）
- カメラ・位置情報・通知などのシステム権限を使う場合、`Info.plist`の`NS***UsageDescription`に利用目的を日本語で明記する

### UIとユーザー体験 (Guideline 4.0)
- Human Interface Guidelines (HIG) に準拠したUIを実装する
- アプリの主要機能はWebViewへの丸投げではなく、ネイティブUIで実装する
- 最低限の機能を持つこと（単なるWebサイトのラッパーはリジェクト対象）
- ローディング状態・エラー状態・空状態を必ずハンドリングする

### パフォーマンスと安定性 (Guideline 2.1)
- クラッシュやフリーズが発生しない品質を保つ
- beta版・テスト用の機能やUIをリリースビルドに含めない
- `console.log`や開発用デバッグコードはリリース前に除去する

### コンテンツとメタデータ (Guideline 2.3 / 4.3)
- アプリの説明・スクリーンショットと実際の機能が一致すること
- 未実装のプレースホルダー画面やダミーコンテンツを残さない

### アプリ内課金 (Guideline 3.1)
- デジタルコンテンツの購入はAppleのIn-App Purchase APIを使用すること（外部決済への誘導は禁止）
- 課金を実装する場合、リストア機能を必ず提供する

### コード品質（審査でのリジェクト防止）
- 非公開API（プライベートAPI）を使用しない
- Expoのmanaged workflowで対応できないネイティブモジュールは`expo-dev-client`経由で安全に導入する
- `app.json` / `app.config.ts`の`bundleIdentifier`、`version`、`buildNumber`を正しく管理する

## Apple Watch (watchOS) 開発ルール

### アーキテクチャ
- watchOSアプリは**Swift (SwiftUI)** で実装する（React Native/Expoはwatchos非対応）
- Expo側はprebuild (`expo prebuild`) してXcodeプロジェクトを生成し、そこにwatchOSターゲットを追加する
- iPhone ⇄ Watch間のデータ同期は **WatchConnectivity** フレームワークを使用する
- Watch側でも単独動作できるよう、ローカルにデータをキャッシュする

### 共有ロジック
- iPhone/Watch間で共有するデータモデルや定数は、共通のSwift Packageまたは共有フォルダにまとめる
- 同期するデータの型定義はTypeScript側とSwift側で齟齬が生じないよう、スキーマを一元管理する

### watchOS固有のUI制約
- 画面サイズが極めて小さいため、1画面1機能を徹底する
- `NavigationStack`ベースのシンプルな画面遷移にする
- テキスト入力は極力避け、タップ・スワイプ・Digital Crownによる操作を前提に設計する
- コンプリケーション対応が必要な場合は`WidgetKit`で実装する

### watchOS審査追加要件 (Guideline 10.0)
- Watch単独で意味のある機能を提供すること（iPhoneアプリへの単なるリモコンはリジェクト対象）
- watchOS向けのスクリーンショットを別途用意する必要がある
- Watch用のプライバシー情報もApp Store Connectで個別に申告する

### Swift コーディング規則
- Swiftコードでも本プロジェクトの原則（不変性、副作用の分離、宣言的記述）を踏襲する
- `let`を優先し、`var`は必要最小限に留める
- コメントは日本語で記述する（TypeScript側と統一）
- 命名はSwift標準のAPI Design Guidelines（camelCase、PascalCase）に従う
